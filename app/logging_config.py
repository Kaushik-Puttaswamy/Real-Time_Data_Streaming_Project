import logging

def configure_logging():
    # Configuring the logging module to display logs at the INFO level
    logging.basicConfig(level=logging.INFO)

# Calling the function to configure logging
configure_logging()
